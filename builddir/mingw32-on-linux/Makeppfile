# Built file for windows releases compiled on linux.

# For cross compilation we need a different toolchain
TOOLCHAIN_PREFIX:=i686-w64-mingw32-
CC:=${TOOLCHAIN_PREFIX}gcc
CXX:=${TOOLCHAIN_PREFIX}g++

# Make it a static application, don't want to ship libraries to windows.
COMMON_FLAGS=
CXXFLAGS+=${COMMON_FLAGS}
LDFLAGS+=${COMMON_FLAGS} -static

# Our release
RELEASE_BINARY_CLI=iteratedecks-cli.exe

$(phony ALL): CLI

include ../cli.Makeppfile
