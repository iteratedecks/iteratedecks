#ifndef ITERATEDECKS_CORE_CARD_FORWARD_HPP
    #define ITERATEDECKS_CORE_CARD_FORWARD_HPP

    namespace IterateDecks {
        namespace Core {

            class Card;

            unsigned short ID2BASE64(UINT const id);

        }
    }

#endif
